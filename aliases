# Tmux Stuff
alias t="tmux"
alias tk="tmux kill-session -t"
alias tl="tmux list-sessions"
alias ta="tmux attach -t"

# Custom configs
# Put custom configs in the file custom-aliases
source ~/configs/custom-aliases

